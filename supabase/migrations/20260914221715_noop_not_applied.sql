-- Migration-history reconciliation.
-- A no-op migration marker was recorded in the canonical Supabase project during launch verification.
-- Keep this file so GitHub remains an exact record of production migration history.
select 1;
